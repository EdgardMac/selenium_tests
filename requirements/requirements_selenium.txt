# Selenium core only - remove problematic packages
selenium==4.15.0

# Lightweight testing
pytest-html==4.0.2
pytest-xdist==3.3.1

# Removed problematic packages:
# pytest-selenium (causes distutils error)
# webdriver-manager (not needed for Termux)